interface BotciiConfig {
    i18nSW: {
        selectedLanguage?: string,
        fallbackLanguage: string
    }
}

const botciiConfig: BotciiConfig = {
    i18nSW: {
        selectedLanguage: 'de',
        fallbackLanguage: 'en'
    }
};

export default botciiConfig